tasks.named('jar') {
    archiveBaseName.set('PSAddonCustomTnt')
}

tasks.named('processResources') {
    filteringCharset = 'UTF-8'
    filesMatching('plugin.yml') {
        expand(
            'name': 'PSAddonCustomTnt',
            'version': project.version
        )
    }
}
